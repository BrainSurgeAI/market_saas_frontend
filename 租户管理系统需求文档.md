# 租户管理系统需求文档

## 1. 现有功能分析

### 1.1 页面组件功能

当前租户管理系统实现了以下功能：

1. **租户列表显示**
   - 以表格形式展示租户信息
   - 支持空数据提示
   - 表格列：ID、名称、类型、状态、地址、创建时间、最后更新、操作

2. **租户详情查看**
   - 通过详情按钮跳转到租户详情页面

3. **新建租户**
   - 表单包含必填字段：租户名称、地址和租户类型
   - 表单验证：
     - 租户名称：不少于6个字符，不超过255个字符
     - 地址：不少于6个字符，不超过64个字符，仅允许特定字符
     - 租户类型：必选，客户或供应商

4. **数据刷新**
   - 手动刷新按钮
   - 创建新租户后自动刷新列表

5. **状态与类型展示**
   - 状态以彩色徽章显示（不同状态不同颜色）
   - 租户类型以徽章显示

6. **界面优化**
   - 表头样式强调
   - 鼠标悬停行高亮
   - 显示总租户数量

### 1.2 数据模型与接口

当前系统使用的数据模型包括：

- **Organization**：包含id、name、nameHash、status、tenantType、address、createdAt、updatedAt等字段

接口包括：
- GET `/api/markets/:market_id/tenants`：获取租户列表
- POST `/api/markets/:market_id/tenants`：创建新租户
- 路由 `/workspace/markets/:market_id/tenants/:nameHash`：租户详情页面

## 2. 改进与拓展需求

### 2.1 核心功能扩展

#### 2.1.1 租户管理增强功能

1. **租户过滤与搜索**
   - **重要度**：高
   - **描述**：提供按名称、类型、状态的搜索和过滤能力
   - **接受标准**：用户能够快速定位特定租户，降低大量数据场景下的管理难度

2. **租户批量操作**
   - **重要度**：中
   - **描述**：支持批量选择租户，进行启用/禁用/删除等操作
   - **接受标准**：能够一次性处理多个租户，提高管理效率

3. **租户编辑功能**
   - **重要度**：高
   - **描述**：允许编辑现有租户的信息
   - **接受标准**：完整的编辑表单，与创建表单一致的字段验证

4. **租户停用/启用**
   - **重要度**：高
   - **描述**：允许临时停用租户账户而不删除数据
   - **接受标准**：停用的租户明显标识，并阻止其访问系统

5. **租户删除功能**
   - **重要度**：中
   - **描述**：支持删除不再需要的租户
   - **接受标准**：删除前有确认提示，防止误操作

#### 2.1.2 租户详情扩展

1. **租户信息卡片**
   - **重要度**：中
   - **描述**：显示完整的租户资料，包括联系人、联系方式等
   - **接受标准**：信息清晰可读，重要信息突出显示

2. **租户活动历史**
   - **重要度**：低
   - **描述**：记录租户的主要活动历史（创建、修改、状态变更等）
   - **接受标准**：时间线形式展示历史记录，支持按时间筛选

3. **租户子账户管理**
   - **重要度**：中
   - **描述**：允许为租户创建多个子账户，分配不同权限
   - **接受标准**：能管理子账户的添加、编辑、删除和权限设置

### 2.2 用户体验优化

1. **分页功能**
   - **重要度**：高
   - **描述**：大量数据时支持分页显示
   - **接受标准**：提供页码导航，可选择每页显示数量

2. **排序功能**
   - **重要度**：中
   - **描述**：允许按各列进行排序（如名称、创建时间）
   - **接受标准**：点击表头可切换升序/降序

3. **导出功能**
   - **重要度**：低
   - **描述**：支持将租户列表导出为Excel或CSV
   - **接受标准**：导出文件包含所有可见字段，格式规范

4. **高级过滤器**
   - **重要度**：中
   - **描述**：提供组合条件的过滤器
   - **接受标准**：可保存常用过滤条件，快速应用

5. **操作反馈优化**
   - **重要度**：中
   - **描述**：提供更明确的操作成功/失败反馈
   - **接受标准**：操作后有明确的状态提示，错误时有补救建议

### 2.3 技术优化

1. **列表性能优化**
   - **重要度**：高
   - **描述**：大量数据时的加载和刷新优化
   - **接受标准**：数据量大时不卡顿，支持增量更新

2. **表单验证增强**
   - **重要度**：中
   - **描述**：更完善的字段验证，如重复检测
   - **接受标准**：实时验证，清晰的错误提示

3. **错误处理机制**
   - **重要度**：高
   - **描述**：完善的错误捕获和恢复机制
   - **接受标准**：用户友好的错误消息，错误日志记录

4. **API请求状态管理**
   - **重要度**：中
   - **描述**：统一管理API请求状态（加载中、成功、失败）
   - **接受标准**：所有请求有一致的加载状态展示

5. **数据缓存策略**
   - **重要度**：低
   - **描述**：实现数据缓存，减少重复请求
   - **接受标准**：优化网络请求数量，提高响应速度

## 3. 详细实现规范

### 3.1 UI/UX规范

1. **布局与响应式设计**
   - 保持现有的卡片容器布局
   - 所有功能需在桌面和平板设备上完整显示
   - 移动设备优化（表格自适应，操作按钮合并）

2. **色彩与图标**
   - 状态色：遵循现有规范（活跃-绿色，待定-黄色，拒绝-红色）
   - 操作图标：使用当前使用的Lucide图标库
   - 确保色彩方案符合无障碍标准

3. **交互设计**
   - 表单交互：实时验证，错误立即显示
   - 列表交互：鼠标悬停高亮，点击条目显示更多操作
   - 确认对话框：危险操作必须二次确认

### 3.2 API设计

1. **租户管理API**
   - `GET /api/markets/:market_id/tenants`：获取租户列表（添加过滤、分页、排序参数）
   - `POST /api/markets/:market_id/tenants`：创建租户
   - `PUT /api/markets/:market_id/tenants/:tenant_id`：更新租户
   - `DELETE /api/markets/:market_id/tenants/:tenant_id`：删除租户
   - `PATCH /api/markets/:market_id/tenants/:tenant_id/status`：更新租户状态

2. **请求/响应格式**
   - 请求体：遵循RESTful设计原则
   - 响应格式：统一的响应结构，包含状态码、消息和数据
   - 错误处理：标准错误代码，详细的错误信息

### 3.3 数据结构扩展

扩展租户数据模型，增加以下字段：
- `contactPerson`：联系人
- `contactPhone`：联系电话
- `email`：邮箱
- `businessLicense`：营业执照号
- `isDisabled`：是否禁用
- `notes`：备注信息

## 4. 实施计划

### 4.1 优先级排序

1. **第一阶段**（核心功能）
   - 租户编辑功能
   - 租户停用/启用
   - 分页功能
   - 列表性能优化

2. **第二阶段**（体验提升）
   - 租户过滤与搜索
   - 排序功能
   - 租户信息卡片
   - 表单验证增强

3. **第三阶段**（功能扩展）
   - 批量操作
   - 租户子账户管理
   - 导出功能
   - 租户活动历史

### 4.2 交付标准

每个功能需满足以下交付标准：
- 完整的功能实现
- 无明显的用户体验问题
- 响应式适配主流设备
- 通过Zod验证的表单数据
- 完整的错误处理
- 符合项目代码规范

### 4.3 测试要求

- **单元测试**：核心功能组件和方法的单元测试
- **集成测试**：API交互的集成测试
- **端到端测试**：核心用户流程的端到端测试
- **性能测试**：大数据量下的性能测试
- **可访问性测试**：确保无障碍使用

## 5. 风险与依赖

### 5.1 潜在风险

- 大量数据下的性能问题
- 多用户同时操作的数据一致性
- 网络环境不稳定时的用户体验
- 后端API变更导致的前端适配问题

### 5.2 依赖关系

- 依赖后端API稳定性
- 依赖权限系统的完善性
- 依赖UI组件库的更新与维护
- 依赖用户反馈完善产品

## 6. 附录

### 6.1 屏幕原型参考

基于现有界面，扩展以下视图：
- 租户列表视图（增加过滤和分页）
- 租户详情视图（完整信息展示）
- 租户编辑表单
- 批量操作界面
- 高级搜索/过滤界面

### 6.2 技术栈与工具

- 前端框架：React + Next.js
- UI组件：继续使用shadcn/ui
- 表单处理：React Hook Form + Zod
- 状态管理：根据复杂度考虑引入Context或Redux
- API请求：Fetch API或考虑使用SWR/TanStack Query 